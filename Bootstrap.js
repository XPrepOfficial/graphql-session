/**
 * bootstrap class runs once app is started
 * */
exports = module.exports = class Bootstrap {

    constructor(app) {
        this.app = app;
        return this.run();
    }

    async run() {

    }

};
